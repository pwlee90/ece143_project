import pandas as pd
from typing import List

def train_random_forest(df : pd.DataFrame, features : List[str]):
    """"
    Params:
        df : a Dataframe to use as training data
        features : a list of strings that contain the name of the features we want to use
    """
    pass
